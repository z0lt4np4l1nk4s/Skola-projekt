@foreach (var item in ViewBag.Skole)
{
    bool found = true;
    foreach (var v in ViewBag.SelectedSkole)
    {
        if (v.IDSkola == item.ID && Model.ID == v.IDDjelatnik)
        {
            found = false;
            <li style="list-style:none"> <input type="checkbox" name="SkolaID" value="@item.ID" checked="checked"  /> @item.Naziv</li>
        }
    }
    if (found)
    {
        <li style="list-style:none"> <input type="checkbox" name="SkolaID" value="@item.ID"  /> @item.Naziv</li>
    }
}
